{
    "name": "a4-name",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "cd a1-backend && npm install && npm start",
        "heroku-postbuild": "cd client &&  npm install && npm run build"
    },
    "engines": {
        "node": "10.15.3"
    }
  }
  